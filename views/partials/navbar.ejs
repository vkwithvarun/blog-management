<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
  <div class="container">
    <a class="navbar-brand fw-bold" href="/">📝 DevBlog</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navMenu">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navMenu">
      <form class="d-flex mx-auto my-2 my-lg-0" method="GET" action="/" role="search">
        <input class="form-control me-2" type="search" name="q" placeholder="Search articles..." value="<%= typeof q !== 'undefined' ? q : '' %>">
        <button class="btn btn-outline-light" type="submit">Search</button>
      </form>
      <ul class="navbar-nav ms-auto align-items-lg-center">
        <% if (currentUser) { %>
          <li class="nav-item"><a class="nav-link" href="/articles/new">+ New Article</a></li>
          <li class="nav-item"><span class="nav-link text-light-emphasis">Hi, <%= currentUser.username %></span></li>
          <li class="nav-item">
            <form action="/logout" method="POST" class="d-inline">
              <button type="submit" class="btn btn-sm btn-outline-light">Logout</button>
            </form>
          </li>
        <% } else { %>
          <li class="nav-item"><a class="nav-link" href="/login">Login</a></li>
          <li class="nav-item"><a class="nav-link" href="/signup">Sign Up</a></li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
